node_modules/
dist/
.next/
coverage/
*.lock
package-lock.json
*.log
# Example files document placeholder values — not real credentials
*.example
# Seed files contain sample UUIDs/hashes that trigger false positives
Database/seeds/
# Migration files contain column names like refresh_token_hash
Database/schemas/
# Spec and plan docs contain example connection strings — not real credentials
docs/
