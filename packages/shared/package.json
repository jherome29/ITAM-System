{
  "name": "@aimrs/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types, interfaces, enums and constants for AIMRS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}
