name: Security

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]
  schedule:
    - cron: '0 0 * * *'   # daily at midnight UTC

permissions:
  contents: read
  security-events: write   # required to upload SARIF to GitHub Security tab
  actions: read

jobs:
  # ── Secret Scan ───────────────────────────────────────────────────────────
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run secretlint
        run: npx secretlint "**/*"

  # ── CodeQL ────────────────────────────────────────────────────────────────
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@f411752efdf656cb71aa17b755b22c890960da1d # v3.35.5
        with:
          languages: javascript-typescript
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@f411752efdf656cb71aa17b755b22c890960da1d # v3.35.5

      - name: Analyze
        uses: github/codeql-action/analyze@f411752efdf656cb71aa17b755b22c890960da1d # v3.35.5
        with:
          category: '/language:javascript-typescript'

  # ── Dependency Audit ──────────────────────────────────────────────────────
  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # npm audit --workspace is unreliable across npm versions.
      # Run from each directory directly for consistent behavior.
      # Threshold restored to high on 2026-08-15: multer 2.2.0 (fixed) shipped,
      # resolving the DoS advisory that forced the critical-only threshold on
      # 2026-06-18. No known-unfixable high/critical CVEs remain as of this date.
      - name: Backend audit (JSON report)
        run: cd Backend && npm audit --audit-level=high --json > /tmp/backend-audit.json || true

      - name: Frontend audit (JSON report)
        run: cd Frontend && npm audit --audit-level=high --json > /tmp/frontend-audit.json || true

      - name: Upload audit reports
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dependency-audit-report
          path: |
            /tmp/backend-audit.json
            /tmp/frontend-audit.json
          retention-days: 30

      # Fails the job on high/critical CVEs. If a future advisory has no
      # upstream fix, add a scoped `npm audit fix --omit` / advisory-specific
      # exception here instead of dropping this threshold globally again.
      - name: Fail on Backend high/critical CVEs
        run: cd Backend && npm audit --audit-level=high

      - name: Fail on Frontend high/critical CVEs
        run: cd Frontend && npm audit --audit-level=high

  # ── OWASP Dependency-Check ────────────────────────────────────────────────
  # continue-on-error: true prevents a slow NVD DB fetch or empty NVD_API_KEY
  # from blocking the security workflow. Remove once the pipeline is stable.
  owasp-dc:
    name: OWASP Dependency-Check
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'
          cache: 'npm'

      # OWASP DC runs in a Docker container that mounts the workspace.
      # node_modules must be present for the Node Audit Analyzer to work.
      - name: Install dependencies
        run: npm ci

      # Build args string — only include --nvdApiKey when the secret is set.
      # Passing --nvdApiKey with an empty value causes a CLI parse error.
      - name: Build OWASP DC args
        id: owasp_args
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: |
          # --disableNodeAudit: the npm audit API rejects monorepo payloads
          # with aliases; npm audit runs separately in the dependency-audit job
          ARGS="--enableRetired --disableNodeAudit"
          if [ -n "$NVD_API_KEY" ]; then
            ARGS="$ARGS --nvdApiKey $NVD_API_KEY"
          fi
          echo "args=$ARGS" >> "$GITHUB_OUTPUT"

      # First run downloads the NVD database (~10 min). Subsequent runs use cache.
      - name: Run OWASP Dependency-Check
        uses: dependency-check/Dependency-Check_Action@1e54355a8b4c8abaa8cc7d0b70aa655a3bb15a6c # main@2025-06-17
        with:
          project: 'AIMRS'
          path: '.'
          format: 'ALL'
          args: ${{ steps.owasp_args.outputs.args }}

      - name: Upload HTML report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: owasp-dc-report
          path: reports/dependency-check-report.html
          retention-days: 30

      - name: Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@f411752efdf656cb71aa17b755b22c890960da1d # v3.35.5
        if: always()
        with:
          sarif_file: reports/dependency-check-report.sarif

  # --- FUTURE: CD JOBS ---
  # Add after Docker image build is configured:
  #
  # trivy-scan:
  #   Scan built Docker images using aquasecurity/trivy-action
  #   Upload SARIF to GitHub Security tab
  #
  # docker-push:
  #   Build and push images to GitHub Container Registry (ghcr.io)
  #   Trigger: push to main only
  #   Requires: GHCR_TOKEN secret
